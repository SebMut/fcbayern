import { BASE_M, MON } from '../FcBayern_Tom/schedule.js';
import './club-kits.js';

// SeasonCrew deliberately does not expose club logo domains.
// The UI renders neutral two-colour jersey icons instead.
export const D=Object.freeze({});
export { BASE_M, MON };
